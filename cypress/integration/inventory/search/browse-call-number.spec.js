import devTeams from '../../../support/dictionary/devTeams';
import testTypes from '../../../support/dictionary/testTypes';

describe('ui-inventory: search', () => {
  before('Creating user and record item with call number', () => {

  });

  after('Deleting user and record', () => {

  });
  it('C358140 Verify that browsing for "call number" with "space" value will get the correct result', { tags : [devTeams.spitfire, testTypes.smoke] }, () => {

  });
});
